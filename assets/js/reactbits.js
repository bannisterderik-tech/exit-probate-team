/* Intentionally minimal — the prior effect layer has been retired to keep
   the site quiet and trustworthy. This stub exists so the script tag
   doesn't 404. The gentle fade reveals are handled by animations.js. */
